# Database
DATABASE_URL="postgresql"

# Server
PORT=3000
NODE_ENV=development

# CORS (comma-separated list of allowed origins for production)
# CORS_ORIGINS="https://yourdomain.com,https://app.yourdomain.com"

CATEGORY_POP=UUID
CATEGORY_RAP=UUID
CATEGORY_ROCK=UUID:UUID
CATEGORY_OTHER=UUID